import { AddPrefixDirective } from './add-prefix.directive';

describe('AddPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new AddPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
