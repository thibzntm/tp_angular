import { DateToSecondsPipe } from './date-to-seconds.pipe';

describe('DateToSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
